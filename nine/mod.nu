use ./commands.nu *

export alias ps = _ps
export alias "into record" = _into_record
export alias clear = _clear
export alias "scope commands" = _scope_commands
export alias "help commands" = _help_commands
